<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Ticketsfoundry" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Iconos de la aplicación %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Bootstrap 5 y Icons %>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>

    <%# Librería de Google Charts (Necesaria para Chartkick) %>
    <script src="https://www.gstatic.com/charts/loader.js"></script>

    <%# Assets de Rails (SOLO UNA VEZ) %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-light">
    <%# Barra de Navegación %>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm">
      <div class="container">
        <a class="navbar-brand fw-bold" href="/">
          <i class="bi bi-tools me-2 text-primary"></i>TicketsFoundry
        </a>
        
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
          <div class="navbar-nav ms-auto align-items-center">
            
            <%= link_to root_path, class: "nav-link me-2" do %>
              <i class="bi bi-speedometer2 me-1"></i> Dashboard
            <% end %>

            <%= link_to tickets_path, class: "nav-link me-2" do %>
              <i class="bi bi-collection-fill me-1"></i> Todos los Tickets
            <% end %>

            <%= link_to new_ticket_path, class: "nav-link me-3" do %>
              <i class="bi bi-plus-lg me-1"></i> Nuevo Ticket
            <% end %>

            <%# DROPDOWN DE ADMINISTRACIÓN %>
            <% if user_signed_in? && current_user.admin? %>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="adminDropdown" role="button" data-bs-toggle="dropdown">
                  <i class="bi bi-shield-lock-fill"></i> Administración
                </a>
                <ul class="dropdown-menu dropdown-menu-dark">
                  <li><%= link_to "Categorías", categories_path, class: "dropdown-item" %></li>
                  <li><%= link_to "Usuarios", users_path, class: "dropdown-item" %></li>
                </ul>
              </li>
            <% end %>

            <% if user_signed_in? %>
              <div class="d-flex align-items-center text-light ms-lg-3 mt-3 mt-lg-0">
                <i class="bi bi-person-circle me-1"></i>
                <span class="small me-3"><%= current_user.name %></span>
                <%= button_to destroy_user_session_path, method: :delete, class: "btn btn-outline-danger btn-sm" do %>
                  <i class="bi bi-box-arrow-right"></i> Salir
                <% end %>
              </div>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <%# Contenedor para Alertas (Flash) %>
    <div class="container mt-3">
      <% flash.each do |type, message| %>
        <div class="alert <%= type == 'notice' ? 'alert-success' : 'alert-danger' %> alert-dismissible fade show shadow-sm" role="alert">
          <i class="bi <%= type == 'notice' ? 'bi-check-circle-fill' : 'bi-exclamation-triangle-fill' %> me-2"></i>
          <%= message %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>
    </div>

    <%# Contenido Principal %>
    <main>
      <%= yield %>
    </main>
  </body>
</html>